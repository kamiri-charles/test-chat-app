import React from 'react';
import './styles.scss';

const ChatWrapper: React.FC = () => {
  return (
    <div className='chat-wrapper'>This is the chat wrapper component.</div>
  );
};

export default ChatWrapper;